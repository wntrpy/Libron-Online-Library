book-borrow-system/
│
├── backend/                # Django project
│   ├── backend/            # Main Django config
│   ├── accounts/           # User mgmt (auth, roles, permissions)
│   ├── members/            # Member-specific features
│   ├── librarians/         # Librarian features
│   ├── admins/             # Admin features
│   ├── books/              # Book catalog, categories
│   ├── borrow/             # Borrowing, returns, fines
│   ├── notifications/      # Email or system notifications
│   └── ...
│
├── frontend/               # React + Vite project
│   ├── src/
│   │   ├── api/            # API calls
│   │   ├── components/     # Shared components
│   │   ├── hooks/          # Custom hooks (auth, fetching)
│   │   ├── layouts/        # Role-based layouts
│   │   ├── pages/
│   │   │   ├── member/     # Member UI
│   │   │   ├── librarian/  # Librarian UI
│   │   │   ├── admin/      # Admin UI
│   │   │   └── auth/       # Login/Register
│   │   ├── router/         # Role-based routing
│   │   └── store/          # Global state (e.g., Zustand/Redux)
│   └── index.html
│
└── docker-compose.yml      # (optional)

# Email configuration
# Set these environment variables (or add them to your process manager)
# so Django can send forgot-password emails through a real SMTP server:
#   EMAIL_BACKEND             # optional, defaults to smtp backend
#   EMAIL_HOST                # e.g., smtp.gmail.com
#   EMAIL_PORT                # e.g., 587
#   EMAIL_HOST_USER           # SMTP username / no-reply address
#   EMAIL_HOST_PASSWORD       # SMTP password or app password
#   EMAIL_USE_TLS             # "true" (default) or "false"
#   EMAIL_USE_SSL             # "true" or "false" (default)
#   DEFAULT_FROM_EMAIL        # optional override for From header


superuser
email: marmildez03@gmail.com
password: Password123321!


member@example.com
 / test1234 (Member)

librarian@example.com
 / test1234 (Librarian)

admin@example.com
 / test1234 (Admin)



//do this kapag may binago sa tables
python manage.py makemigrations
python manage.py migrate

member@gmail.com
member123


armildezmoses11olpstem@gmail.com
kyejWFEdjM


admin@gmail.com
admin123

librarian@gmail.com
lib123